import React from 'react';

function Header() {
    return (
        <header>
            <h1>
                <a href="/">Validators Efficiency</a>
            </h1>
        </header>
    );
}

export default Header;
